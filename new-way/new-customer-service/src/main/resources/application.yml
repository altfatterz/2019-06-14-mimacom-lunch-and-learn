spring:
  application:
    name: new-customer-service

logging:
  level:
    org.springframework: info
    io.github.resilience4j: debug

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include:
          - info
          - health
          - metrics
          - prometheus



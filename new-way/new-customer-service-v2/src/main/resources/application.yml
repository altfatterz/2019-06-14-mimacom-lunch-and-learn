spring:
  application:
    name: new-customer-service-v2

logging:
  level:
    io.github.resilience4j: debug

server:
  port: 6060

management:
  endpoints:
    web:
      exposure:
        include:
          - info
          - health
          - metrics
          - prometheus
